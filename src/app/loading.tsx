export default function Loading() {
  return (
    <div className="flex min-h-screen animate-pulse items-center justify-center bg-primary/5">
      <span className="sr-only">Cargando...</span>
    </div>
  );
}
